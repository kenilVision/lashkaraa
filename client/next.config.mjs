/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['www.lashkaraa.in','scontent.cdninstagram.com','images.pexels.com'],
    },
};

export default nextConfig;
